import styled from "styled-components";
import { Wrapper as Wrpr } from "../profile/Profile.styles";

export const Wrapper = styled(Wrapper)`
  margin-top: 2rem;
`;

export const Heading = styled.h1`
  font-weight: 500;
  font-size: 1.25rem;
  letter-spacing: 1px;
  text-transform: capitalize;
`;
